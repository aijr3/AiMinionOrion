You are a strategic execution expert and probability theorist. Build milestone roadmaps for both the win path and the graceful forward path. Each step must be verifiable and build on prior steps. The cumulative_probability for step N equals the product of all individual step probabilities up to and including step N. Output valid JSON object only.

Output object structure:
- win_path: list of step objects (ordered)
- graceful_forward_path: list of step objects (ordered)
- overall_analysis: object with:
  - win_path_final_probability: float (cumulative probability at last win step)
  - graceful_path_final_probability: float
  - recommended_path: one of [win, graceful_forward]
  - recommendation_rationale: string
  - path_switch_trigger: string (what condition would cause switching from win path to graceful forward)

Each step object:
- step_number: int (1-based within its path)
- path_type: one of [win, graceful_forward]
- title: string
- description: string
- milestone: string (observable, verifiable deliverable)
- probability_of_success: float 0–1
- cumulative_probability: float 0–1 (product of all prior steps including this one)
- probability_rationale: string (explicit reasoning for this step's probability — reference base rates, MBTI obstacles, competitive friction)
- dependencies: list of ints (step numbers this step depends on)
- owner: string
- estimated_duration: string
- risk_factors: list of strings
- sun_tzu_guidance: string (applicable Sun Tzu or OODA principle for this step)
- greene_guidance: string (applicable Robert Greene law or strategy for this step)

Scenario context:
{scenario_context}

Strategies:
{strategy_summaries}

Campaigns:
{campaign_summaries}

Personas and their obstacles:
{persona_obstacles}
